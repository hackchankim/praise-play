# 찬양 반주 생성 앱 (PraisePlay) 개발 로드맵

코드 악보 사진 한 장으로 다악기 반주를 만들고, 예배 중 어떤 순서로 진행해도 끊김 없이 이어지는 실시간 반주 서비스.

## 개요

PraisePlay는 매주 예배 반주를 직접 준비해야 하는 **교회 찬양 인도자와 워십팀**을 위한 자동 반주 생성·실시간 재생 서비스로 다음 기능을 제공합니다:

- **악보 자동 판독**: 리드시트 이미지 4~5장을 비전 LLM으로 분석해 가사·코드·조표·마디/박자 구조를 JSON으로 추출 (F001, F002, F013)
- **자동 교정 보조와 수동 교정**: 코드 문법 자동 검증과 섹션 자동 추론으로 초안을 만들고, 원본 이미지와 나란히 보며 인도자가 최종 확정 (F003, F004, F005)
- **다악기 편곡 자동 생성**: 장르 프리셋 기반으로 피아노/기타/베이스/드럼 반주 트랙을 규칙 기반으로 생성 (F006)
- **실시간 구간 전환 재생**: 예배 중 다음 섹션 / 구간 반복 / 특정 섹션 점프 / 다음 곡을 무음 간격 없이 수행 (F007, F008, F009)
- **세트리스트 운영**: 그날 부를 곡을 순서대로 배치하고, 재생 전 사전 로딩과 재생 안정성을 보장 (F011, F012, F014, F015)

> **용어**: 이 문서·데이터 모델·코드에서는 `Setlist` 엔티티를 "세트리스트"로 표기하지만, 실제 앱 UI에는
> "찬양콘티"라는 문구로 노출된다(교회 찬양팀에 더 익숙한 표현). 자세한 내용은 `docs/PRD.md`의
> "용어" 섹션 참고.

### 기술 스택

| 레이어 | 선택 |
|---|---|
| 프레임워크 | Next.js 16 (App Router) + React 19 + TypeScript 5.6+ |
| 스타일링/UI | TailwindCSS v4 + shadcn/ui + Lucide React |
| 폼/검증 | React Hook Form 7.x + Zod |
| 오디오 | Web Audio API + `smplr` **v1.0.0 고정** (내장 시퀀서) |
| 악보 추출 | Claude Vision (Sonnet 5) + Structured Outputs |
| 편곡 엔진 | Tonal.js (Voicing / Voice-Leading) |
| 백엔드 | Next.js Route Handlers + Supabase (PostgreSQL) |
| 비동기 잡 | Inngest (추출, 편곡 생성) |
| 스토리지/인증 | Cloudflare R2 (presigned PUT, 업로드 이미지 저장 전용) + Clerk |
| 배포/패키지 | Vercel / npm |

### MVP 범위 밖 (명시적 제외)

멀티테넌시·조직 관리, 결제/구독, 프로필 상세 관리, 설정(테마·언어·알림), 오프라인 캐싱/PWA, 네이티브 앱, 세트리스트 공유·협업, MIDI 파일 내보내기/다운로드.

> **참고**: 편곡 결과는 표준 MIDI 파일이 아닌 beat 단위 노트 이벤트 JSON(`InstrumentTrack.notes`)으로 Postgres에 저장되며, `smplr` 재생 엔진이 이를 직접 소비합니다. 이 JSON은 음높이·길이·세기·타이밍 정보를 모두 포함하므로, 향후 MIDI 파일 내보내기가 필요해지면 스키마 변경 없이 별도 변환 함수만 추가하면 됩니다.

---

## 개발 워크플로우

1. **작업 계획**

- 기존 코드베이스를 학습하고 현재 상태를 파악
- 새로운 작업을 포함하도록 `ROADMAP.md` 업데이트
- 우선순위 작업은 마지막 완료된 작업 다음에 삽입

2. **작업 생성**

- 기존 코드베이스를 학습하고 현재 상태를 파악
- 고수준 명세서, 관련 파일, 수락 기준, 구현 단계 포함
- API/비즈니스 로직 작업 시 "## 테스트 체크리스트" 섹션 필수 포함 (Playwright MCP 테스트 시나리오 작성)
- 새 작업 문서에는 빈 박스와 변경 사항 요약이 없어야 함

3. **작업 구현**

- 작업 파일의 명세서를 따름
- 기능과 기능성 구현
- API 연동 및 비즈니스 로직 구현 시 Playwright MCP로 테스트 수행 필수
- 각 단계 후 작업 파일 내 단계 진행 상황 업데이트
- 구현 완료 후 Playwright MCP를 사용한 E2E 테스트 실행
- 테스트 통과 확인 후 다음 단계로 진행
- 각 단계 완료 후 중단하고 추가 지시를 기다림

4. **로드맵 업데이트**

- 로드맵에서 완료된 작업을 ✅로 표시

> **알림**: Claude는 매 응답을 마치고 사용자 입력을 기다리기 직전에 그 턴에서 한 일을 한 줄로 요약해
> `PushNotification` 도구로 보낸다(`CLAUDE.md` 지침). 단, 사용자가 터미널을 보고 있는 동안은 harness가
> 중복 알림으로 판단해 실제 전송을 건너뛸 수 있다 — 주로 자리를 비운 것으로 보이는 상황(백그라운드
> 작업 완료 등)에 실제로 전달된다.

---

## 브랜치 전략

Structure-First 접근법상 Phase 3에서 목 데이터 계층이 실제 API/DB로 전면 교체되므로, Phase 단위의 장기 브랜치 대신 **Task 단위의 짧은 생명주기 브랜치**를 사용한다. 머지를 잦고 작게 유지해, 충돌 위험이 가장 큰 전환 시점(Phase 2 → 3)에 거대한 병합이 몰리지 않도록 한다.

### 브랜치 이름 규칙

| 종류 | 패턴 | 예시 |
|---|---|---|
| Task 작업 | `task/NNN-슬러그` | `task/003-domain-types` |
| 버그 수정 | `fix/NNN-슬러그` | `fix/016-extraction-retry` |
| 문서/설정 | `chore/슬러그` | `chore/branch-strategy-docs` |

번호(NNN)는 이 문서의 실제 Task ID와 항상 일치시킨다.

### Task 하나당 흐름

1. `main`에서 최신 상태로 분기 (`/branch` 커맨드)
2. 브랜치 안에서 자유롭게 커밋하며 구현, 해당 Task의 "완료 기준"(API/비즈니스 로직 Task는 테스트 체크리스트 포함)을 충족하는지 확인
3. `npx tsc --noEmit` / `npm run lint` / `npm run format:check` / `npm run build` 통과 확인
4. 같은 브랜치에서 `ROADMAP.md`의 해당 Task를 ✅로 표시하는 커밋 추가
5. 브랜치를 push하고 GitHub PR을 생성해 즉시 squash merge, PR 제목은 `Task NNN: <제목>` 형식 유지 (`/merge` 커맨드) — 병합은 로컬 `git merge`가 아니라 항상 GitHub PR을 통해서만 일어난다
6. 로컬 `main`을 pull로 최신화하고, 로컬(및 원격) 브랜치 삭제

### Phase 경계

Phase 단위 브랜치는 만들지 않는다. 한 Phase의 모든 Task가 `main`에 병합되면 `git tag phase-N-done` 형태의 가벼운 태그만 남긴다.

### main 보호 기준

CI(Task 029)가 갖춰지기 전까지는 merge 직전 로컬에서 `build`/`tsc`/`lint`/`format:check` 통과를 필수 게이트로 삼는다.

---

## 개발 단계

### Phase 1: 애플리케이션 골격 구축

전체 라우트·타입·스키마 골격을 먼저 세워 이후 UI팀과 백엔드팀이 병렬로 움직일 수 있는 기반을 만듭니다. 이 Phase에서는 **실제 비즈니스 로직을 구현하지 않습니다.**

- **Task 001: 프로젝트 초기화 및 개발 환경 구성** ✅
  - `npm create next-app`으로 Next.js 16 App Router + TypeScript 5.6+ + React 19 프로젝트 생성
  - TailwindCSS v4(설정파일 없는 엔진) 및 `shadcn/ui` init, Lucide React 설치
  - ESLint / Prettier / tsconfig strict 모드 및 path alias(`@/*`) 설정
  - 의존성 설치: `zod`, `react-hook-form`, `@hookform/resolvers`, `tonal`, `smplr@1.0.0`(정확히 고정)
  - `.env.example` 작성 (Clerk, Supabase, R2, Inngest, Anthropic API 키 자리)
  - `README.md`에 로컬 실행 절차 정리, `npm run dev` / `npm run build` 무오류 확인
  - 완료 기준: 빈 프로젝트가 로컬에서 기동되고 프로덕션 빌드가 통과

- **Task 002: 라우트 구조 및 공통 레이아웃 골격 구현** ✅
  - App Router 기반 9개 페이지의 빈 껍데기 생성
    - `(auth)/sign-in`, `(auth)/sign-up`
    - `(app)/` 홈, `(app)/songs/upload`, `(app)/songs/[songId]/extracting`, `(app)/songs/[songId]/correction`, `(app)/songs/[songId]/arrangement`
    - `(app)/setlists/[setlistId]`, `(app)/setlists/[setlistId]/play`
  - 라우트 그룹별 `layout.tsx` 분리 (인증 레이아웃 / 앱 셸 레이아웃 / 재생 전용 풀스크린 레이아웃)
  - 앱 셸: 헤더(로고, 홈·세트리스트 링크, 로그아웃 자리), 모바일 하단 내비게이션 골격
  - 전역 `loading.tsx`, `error.tsx`, `not-found.tsx` 배치
  - 페이지 간 이동 경로를 상수화한 `src/lib/routes.ts` 작성
  - 완료 기준: 9개 페이지 URL이 모두 응답하고, 앱 셸 내비게이션만으로 전체 사용자 여정을 클릭으로 순회 가능

- **Task 003: 도메인 타입 정의 및 Supabase 스키마 설계** ✅
  - `src/lib/song-model/types.ts`에 PRD 데이터 모델 전체를 TypeScript 인터페이스로 정의
    - `User`, `Song`, `Section`, `Line`, `ChordEvent`, `Arrangement`, `InstrumentTrack`, `Setlist`, `SetlistItem`
  - 타이밍 규약 명문화: **모든 타이밍은 beat 단위로 저장**하고 재생 시점에만 초로 환산
    - `Section.start_beat`(곡 기준 절대), `Line.start_beat`(섹션 기준 상대), `ChordEvent.beat_offset`(줄 기준 상대)
  - 열거형 정의: `SongStatus`(draft/extracted/corrected), `SectionType`(verse/chorus/bridge/interlude/intro/outro), `Instrument`(piano/guitar/bass/drums), `GenrePreset`(praise_upbeat/ccm_ballad/hymn_traditional/acoustic_intimate)
  - `InstrumentTrack.notes` 노트 이벤트 JSON 스키마 정의 (`{ beat, pitch, duration, velocity }[]` — smplr Sequencer 투입 가능한 형태)
  - 비전 LLM 추출 결과 Zod 스키마 정의 (텍스트 추출용 / 마디·박자 구조 추출용 2종 분리)
  - API 요청·응답 타입과 공통 에러 응답 타입 정의 (`src/lib/api/contracts.ts`)
  - Supabase 마이그레이션 SQL 초안 작성 (`supabase/migrations/` — 테이블·FK·인덱스 정의만, 실제 적용은 Task 013)
  - 완료 기준: `tsc --noEmit` 통과, 모든 페이지와 엔진이 참조할 단일 타입 소스가 확정됨

- **Task 004: 외부 서비스 클라이언트 골격 및 설정 배선** ✅
  - 환경 변수 런타임 검증 모듈 작성 (`src/lib/env.ts`, Zod 기반 — 누락 시 부팅 시점에 실패)
  - Clerk 설치 및 Provider 배선, **Next.js 16의 `proxy.ts`(구 `middleware.ts`) 파일명 규약 확인** 후 보호 라우트 매처 골격 작성 (실제 리디렉션 로직은 Task 014)
  - Supabase 클라이언트 팩토리 스텁 (서버/브라우저 분리, Clerk Third-Party Auth 연동 자리 확보)
  - Cloudflare R2 S3 호환 클라이언트 스텁 및 CORS/presigned PUT 정책 문서화
  - Inngest 클라이언트 및 `/api/inngest` 라우트 핸들러 등록, 헬스체크용 더미 함수 1개
  - Anthropic SDK 클라이언트 스텁 (모델 ID·Structured Outputs 옵션 상수화)
  - 완료 기준: `npx inngest-cli dev`로 함수가 인식되고, 각 클라이언트가 빌드 타임 에러 없이 임포트됨

---

### Phase 2: UI/UX 완성 (더미 데이터 활용)

모든 화면을 **하드코딩된 더미 데이터**로 완성합니다. 백엔드 없이도 전체 사용자 여정을 체험할 수 있게 만들어, Phase 3의 API 교체 범위를 데이터 레이어 한 곳으로 국한시킵니다.

- **Task 005: 디자인 시스템 및 공통 컴포넌트 라이브러리 구축** ✅
  - 디자인 토큰 정의 (색상·타이포·간격·radius) — **예배 현장 저조도 환경을 고려해 다크를 기본값으로 하되, 라이트 테마도 함께 지원**
  - shadcn/ui 컴포넌트 도입: Button, Card, Input, Select, Dialog, Sheet, Tabs, Badge, Progress, Skeleton, Toast, Dropdown, Tooltip
  - 도메인 공통 컴포넌트: `SongCard`, `SetlistCard`, `SectionBadge`, `ChordChip`, `EmptyState`, `PageHeader`, `LoadingState`, `ErrorState`
  - **실시간 재생용 대형 터치 타깃 규격 확립** (최소 64px 높이 버튼, 원거리 판독 가능한 폰트 스케일)
  - 폼 공통 래퍼 구현 (React Hook Form + Zod resolver + 필드 에러 표시 패턴)
  - 완료 기준: 컴포넌트 갤러리 페이지(`/_dev/ui`)에서 전 컴포넌트가 라이트/다크 양쪽에서 정상 렌더링

- **Task 006: 더미 데이터 팩토리 및 목 데이터 액세스 계층 구현** ✅
  - 실제 한국 찬양 1~2곡 기준의 더미 `Song` + `Section` + `Line` + `ChordEvent` 트리 작성 (절/후렴/브릿지 포함, `needs_review` 사례 포함)
  - 더미 `Arrangement` + `InstrumentTrack`(4악기 노트 이벤트) 및 더미 `Setlist`/`SetlistItem` 3곡 세트 작성
  - **데이터 액세스 인터페이스(`SongRepository`, `SetlistRepository`, `ArrangementRepository`) 정의 후 목 구현체 제공**
    - Phase 3에서 인터페이스는 그대로 두고 구현체만 Supabase 버전으로 교체
  - 추출 진행 상태 시뮬레이터 (단계별 진행률을 시간 경과에 따라 방출하는 목 스트림)
  - 완료 기준: 모든 페이지가 목 리포지토리만 사용하며, 리포지토리 인터페이스가 Phase 3 API 계약과 1:1 대응

- **Task 007: 인증 및 홈 화면 UI 구현** ✅
  - 로그인 페이지 UI: 이메일/비밀번호 입력, 검증 메시지, 회원가입 링크, 로그인 버튼
  - 회원가입 페이지 UI: 이름/이메일/비밀번호 입력, Zod 검증, 로그인 링크, 가입하기 버튼
  - 홈 UI: 저장된 곡 목록 그리드(상태 배지 — 임시저장/추출완료/교정완료), 세트리스트 목록
  - 홈 CTA: **악보 업로드** 버튼, **새 세트리스트 만들기** 버튼
  - 곡 클릭 → 편곡 설정, 세트리스트 클릭 → 세트리스트 구성으로 이동하는 네비게이션 배선
  - 빈 상태(곡 0개 / 세트리스트 0개) 및 스켈레톤 로딩 UI
  - 완료 기준: 로그인 → 홈 → 각 진입점으로의 이동이 더미 데이터로 완결

- **Task 008: 악보 업로드 및 추출 진행 화면 UI 구현** ✅
  - 업로드 페이지: 드래그앤드롭 + 파일 선택, 다중 이미지 미리보기 썸네일
  - 이미지 순서 조정(드래그 정렬) 및 개별 제거, 4~5장 권장 안내 및 개수/용량/포맷 클라이언트 검증
  - **업로드 및 추출 시작** 버튼과 업로드 진행률 표시 UI
  - 추출 진행 페이지: 이미지별 추출 진행률 카드, 전체 단계 표시(업로드 → 텍스트 추출 → 구조 추출 → 병합 → 검증)
  - 추출 실패 상태 UI 및 **재시도** / **업로드 페이지로 돌아가기** 액션
  - 완료 기준: 목 시뮬레이터로 업로드 → 진행 → 완료 → 교정 페이지 자동 이동 흐름이 재생됨

- **Task 009: 교정 페이지 UI 구현** ✅ *(MVP 최대 UI 난이도)*
  - 좌우 분할 레이아웃: 좌측 원본 이미지 뷰어(확대/축소/페이지 이동), 우측 추출 결과 에디터 (모바일에서는 탭 전환)
  - 가사 줄 에디터: 인라인 텍스트 수정, 줄 추가/삭제/순서 변경
  - **코드 칩 배치 에디터**: 가사 문자 오프셋 위에 코드 칩을 드래그로 이동, 코드 기호 인라인 편집, 추가/삭제
  - `needs_review` 코드의 시각적 강조 및 "검토 필요 N건" 필터/일괄 순회 UI
  - 마디 경계·줄별 박자 수·반복(도돌이표) 구간 확인 및 수정 UI
  - 섹션 라벨 재지정 드롭다운(1절/2절/후렴/브릿지/간주/인트로/아웃트로) 및 섹션 병합·분할
  - 곡 메타 확정 폼: 키(조표) / 템포(BPM) / 박자표
  - **저장** 및 **임시 저장 후 나가기** 액션, 미저장 이탈 경고 다이얼로그
  - 완료 기준: 더미 곡에 대해 코드 이동·가사 수정·섹션 재라벨·메타 확정이 모두 UI상에서 동작

- **Task 010: 편곡 설정 및 세트리스트 구성 화면 UI 구현** ✅
  - 편곡 설정 페이지: 장르 프리셋 카드 선택(경쾌한 찬양 / CCM 발라드 / 전통 찬송 / 어쿠스틱)
  - 프리셋별 악기 구성 미리보기(피아노·기타·베이스·드럼 아이콘 및 섹션별 밀도 요약)
  - 반주 미리듣기 플레이어 UI(재생/일시정지/진행바 — 실제 오디오는 Phase 3에서 연결)
  - **세트리스트에 추가** / **저장만 하고 나중에 사용** 분기 버튼
  - 세트리스트 구성 페이지: 세트리스트 이름 편집, 저장된 곡 검색·추가 모달
  - 곡 순서 드래그 정렬, 곡 제거, 곡별 사용할 편곡 선택, **예배 시작** 버튼
  - 완료 기준: 편곡 설정 → 세트리스트 추가 → 순서 배치 → 예배 시작 클릭까지의 흐름이 더미로 완결

- **Task 011: 실시간 재생 화면 UI 구현** ✅
  - 사전 로딩 화면: 악기 사운드/트랙 데이터별 로딩 진행률과 완료 상태 표시, 완료 시 자동 진행
  - 재생 화면 상단: 현재 곡 제목·키·템포, 세트리스트 내 위치(3곡 중 2번째)
  - **가사 중심 내비게이션**: 정적인 "다음 섹션/점프/다음 곡" 버튼 대신, **세트리스트 전곡 가사를 하나로
    이어붙인 연속 스크롤**을 메인 콘텐츠로 삼아 가사 자체가 이동 수단이 되게 한다
    - 재생 중인 곡은 펼쳐서 전체 가사를 보여주고, 다른 곡은 곡 제목만 접어서 표시(탭하면 펼침)
    - 절/후렴 등 섹션 배지를 탭하면 같은 곡이든 다른 곡이든 구분 없이 그 지점으로 즉시 이동
    - 지금 부르는 줄을 강조하고 자동 스크롤하며, 둘러보다가도 **현재 재생 위치로 즉시 복귀**하는
      버튼을 둔다
    - **재생 중인 곡의 섹션만 모아 보여주는 빠른 이동 칩 한 줄**을 가사 스크롤 위 고정 위치에
      둔다 — 4절을 부르다 1절로 돌아가는 등 몇 섹션 떨어진 지점으로 갈 때 스크롤해서 찾을 필요
      없이 즉시 탭 가능
  - **구간 반복**: 전역 토글 버튼 대신 재생 중인 섹션 배지에만 붙는 작은 아이콘으로 표시(주로 후렴
    반복에 사용) — 반복이 의미 있는 순간에만 노출
  - **재생/일시정지만 유일하게 고정으로 남는 컨트롤** — 작은 원형 버튼. 가사 스크롤 영역 위에
    떠 있는 오버레이가 아니라 하단에 구분선으로 분리된 별도 영역에 둬서, 스크롤 중 손가락에
    가려지거나 오조작될 위험을 없앤다. 나머지는 모두 가사를 탭하는 동작으로 대체
  - **대기 중 전환 상태 표시**(마디 경계 정렬 대기 중임을 알리는 펜딩 인디케이터, 예약 취소 가능)를
    탭한 지점 근처에 표시
  - 화면 꺼짐 방지 활성 상태 표시, 예배 종료 → 홈 복귀 액션
  - 완료 기준: 더미 타이머 기반으로 가사 탭을 통한 절 이동(같은 곡/다른 곡)·후렴 반복·다음 곡 전환의
    UI 상태 변화가 모두 확인 가능

- **Task 012: 반응형·접근성 및 전체 사용자 플로우 검증** ✅
  - 모바일(예배 현장 주 사용 환경) / 태블릿 / 데스크톱 3개 브레이크포인트 대응 점검
  - 실시간 재생 페이지의 **가로/세로 모드 및 한 손 조작** 최적화
  - 키보드 내비게이션, 포커스 링, ARIA 라벨, 색 대비(WCAG AA) 점검
  - PRD 사용자 여정 10단계를 처음부터 끝까지 클릭으로 완주하는 플로우 검증
  - 페이지 전환 시 로딩/에러/빈 상태 일관성 점검
  - 완료 기준: 3개 뷰포트에서 전체 여정 완주 및 레이아웃 깨짐 0건

---

### Phase 3: 핵심 기능 구현

목 리포지토리를 실제 API·엔진으로 교체하고, MVP의 기술적 심장인 **추출 파이프라인**과 **실시간 재생 엔진**을 구현합니다.

- **Task 013: Supabase 스키마 마이그레이션 및 데이터 액세스 계층 구현** - 우선순위 ✅
  - PRD 데이터 모델 9개 테이블 마이그레이션 적용
    - `users`, `songs`, `sections`, `lines`, `chord_events`, `arrangements`, `instrument_tracks`, `setlists`, `setlist_items`
  - FK 및 자기참조 처리: `sections.repeat_target_section_id → sections.id`, `setlist_items.arrangement_id → arrangements.id`
  - 조회 인덱스: `songs(created_by, created_at)`, `sections(song_id, order_index)`, `lines(section_id, order_index)`, `chord_events(line_id)`, `setlist_items(setlist_id, order_index)`
  - 사용자 소유권 기반 RLS 정책 적용 (`created_by` / `owner_id` 스코프)
  - Task 006의 리포지토리 인터페이스에 대한 **Supabase 구현체 작성 및 목 구현체 교체**
  - 곡 전체 트리(Song → Section → Line → ChordEvent)를 1회 왕복으로 읽는 조회 함수 및 트랜잭션 기반 일괄 저장 함수
  - 시드 스크립트로 더미 곡을 실제 DB에 적재
  - 완료 기준: 9개 테이블 마이그레이션이 적용되고, 시드 스크립트로 적재한 더미 곡이 Supabase 구현체를 통해 손실 없이 재조회됨

  #### 테스트 체크리스트
  - [ ] 곡 트리 저장 후 재조회 시 beat 값과 순서가 손실 없이 보존되는가
  - [ ] 섹션 자기참조(반복 대상) 저장·조회가 순환 없이 동작하는가
  - [ ] 다른 사용자의 곡/세트리스트가 RLS로 차단되는가
  - [ ] 저장 중 실패 시 트랜잭션 롤백으로 부분 저장이 남지 않는가
  - [ ] Playwright MCP: 홈 접속 → 실제 DB의 시드 곡 목록이 렌더링되는지 검증

- **Task 014: Clerk 인증 연동 및 접근 제어 구현** ✅
  - Clerk Provider 및 로그인/회원가입 UI를 실제 Clerk 인증 흐름에 연결
  - Next.js 16 `proxy.ts`에서 보호 라우트 매처 구성, **비로그인 홈 접근 시 로그인 페이지 자동 리디렉션**
  - Clerk `user_id` ↔ `users` 테이블 동기화(최초 로그인 시 레코드 생성, `display_name`, `created_at`)
  - Clerk Third-Party Auth로 Supabase RLS에 사용자 컨텍스트 전달
  - 로그아웃 및 세션 만료 처리, 모든 Route Handler의 인증 가드 유틸 작성
  - 로그인/가입 실패 시 에러 메시지 표시
  - 완료 기준: 비로그인 사용자가 보호 라우트 접근 시 로그인 페이지로 리디렉션되고, 로그인 후 Supabase RLS가 사용자 컨텍스트를 정확히 인식함

  #### 테스트 체크리스트
  - [ ] 비로그인 상태로 각 보호 라우트 직접 접근 시 로그인 페이지로 리디렉션되는가
  - [ ] 로그인 성공 시 홈으로, 실패 시 에러 메시지가 표시되는가
  - [ ] 로그아웃 후 뒤로가기로 보호 페이지에 재진입되지 않는가
  - [ ] 사용자 A가 사용자 B의 `songId` URL로 직접 접근 시 차단되는가
  - [ ] Playwright MCP: 회원가입 → 홈 → 로그아웃 → 홈 재접근 리디렉션 전체 플로우 E2E 검증

- **Task 015: R2 이미지 업로드 파이프라인 구현 (F001)** ✅
  - presigned PUT URL 발급 Route Handler (`POST /api/uploads/presign`) — 파일 타입·크기 검증 포함
  - **R2는 브라우저 HTML form POST를 지원하지 않으므로 presigned PUT + CORS 설정 방식으로 구현**
  - 클라이언트 직접 업로드 로직 및 이미지별 진행률·개별 재시도
  - 업로드 완료 시 `songs` 레코드 생성(`status: draft`)과 이미지 키·순서 등록
  - 업로드 실패·부분 실패 롤백 처리 및 고아 객체 정리 전략
  - 이미지 리사이즈/압축으로 비전 LLM 입력 토큰 절감
  - 완료 기준: 4~5장 이미지가 R2에 업로드되고 DB에 순서·키가 정확히 등록되며, 실패 이미지 재시도가 정상 동작함

  #### 테스트 체크리스트
  - [ ] 4~5장 업로드 후 R2 객체 키와 DB 이미지 순서가 일치하는가
  - [ ] 허용되지 않은 확장자·초과 용량 파일이 서버에서 거부되는가
  - [ ] 업로드 중 1장 실패 시 재시도로 복구되고 순서가 유지되는가
  - [ ] presign URL이 타 사용자에게 발급되지 않는가(인증 가드)
  - [ ] Playwright MCP: 업로드 페이지에서 다중 파일 선택 → 순서 변경 → 제출 → 추출 진행 페이지 이동까지 E2E 검증

- **Task 016: 비전 LLM 악보 추출 파이프라인 구현 (F002, F013)**
  - Inngest 잡 `extract-chart` 구현 (`src/jobs/extract-chart.ts`)
  - **호출 2종 분리**: (1) 가사/코드/조표 텍스트 추출, (2) 마디 경계·줄별 박자 수·도돌이표 구조 추출
    - `docs/PLAN.md`의 정확도 유의사항 반영 — 텍스트 판독과 공간 추론/카운팅의 신뢰도가 다르므로 프롬프트와 재시도 정책을 분리
  - Anthropic `output_config.format`(Structured Outputs)으로 Zod 스키마 강제
  - 이미지별 추출 → **다중 이미지 병합/정렬 단계**(페이지 경계에서 잘린 줄·섹션 이어붙이기)
  - self-consistency 체크: 구조 추출 2회 호출 후 불일치 항목에 `needs_review` 플래그
  - 실패 시 단계별 재시도·백오프, 부분 실패 시 성공분 보존
  - 추출 결과를 `sections`/`lines`/`chord_events`로 영속화, `songs.status → extracted`
  - 완료 기준: 실제 리드시트 이미지 업로드 시 가사/코드/조표/마디·박자 구조가 스키마에 맞게 추출·병합되어 DB에 영속화되고 `songs.status`가 `extracted`로 전환됨

  #### 테스트 체크리스트
  - [ ] 실제 리드시트 이미지로 가사·코드·조표가 스키마에 맞게 추출되는가
  - [ ] 스키마 위반 응답이 재시도 후에도 실패할 때 잡이 안전하게 실패 상태로 종료되는가
  - [ ] 여러 이미지에 걸친 곡이 순서대로 하나의 섹션 트리로 병합되는가
  - [ ] 구조 추출 불일치 시 `needs_review`가 정확히 마킹되는가
  - [ ] 잡 타임아웃·API 429 발생 시 재시도 정책이 동작하는가
  - [ ] Playwright MCP: 업로드 → 추출 진행 페이지에서 실시간 진행률 갱신 → 완료 시 교정 페이지 자동 이동 E2E 검증
  - [ ] Playwright MCP: 추출 실패 케이스에서 재시도 버튼 동작 검증

- **Task 017: 코드 문법 검증기 및 섹션 자동 추론 구현 (F003, F004)**
  - **결정론적(비-LLM) 코드 문법 검증기** 구현 (`src/lib/song-model/chord-validator.ts`)
    - 문법: `^[A-G](#|b)?(maj7|m7|m|7|sus2|sus4|dim7?|aug|add9|6|9|11|13)?(/[A-G](#|b)?)?$`
    - **위첨자 표기(Dsus⁴, C⁶) 정규화 전처리** 포함
    - 검증 실패 시 `chord_events.needs_review = true`
  - Tonal.js로 파싱 가능 여부 교차 검증 및 조표 대비 이례적 코드 경고
  - 섹션 자동 추론기 구현 (`src/lib/song-model/infer-sections.ts`)
    - 가사 줄 반복 패턴 + 코드 진행 반복 + 도돌이표 위치 기반 절/후렴/브릿지/간주 라벨링
    - "라라라" 등 보칼리제이션 구간을 간주로 추론
    - `Section.order_index`와 `repeat_target_section_id` 산출
  - 추출 잡 후단에 검증·추론 단계 연결
  - 대표 곡 케이스로 유닛 테스트 작성 (정확도 100% 전제 아님 — 교정 UI가 최종 방어선)
  - 완료 기준: 추출 결과에 대해 코드 문법 검증과 섹션 자동 추론이 자동 실행되어 `needs_review` 플래그와 섹션 라벨이 정확히 산출됨

  #### 테스트 체크리스트
  - [ ] 유효 코드 50종 / 무효 코드 20종에 대한 검증 결과가 기대와 일치하는가
  - [ ] 위첨자·유니코드 변형 코드가 정규화 후 통과하는가
  - [ ] 슬래시 베이스 코드(`G/B`)와 복합 코드(`Cmaj7/E`)가 처리되는가
  - [ ] 반복 구조를 가진 곡에서 후렴이 동일 섹션으로 묶이는가
  - [ ] 도돌이표가 `repeat_target_section_id`로 올바르게 변환되는가
  - [ ] Playwright MCP: 교정 페이지에서 `needs_review` 코드가 강조 표시되고 "검토 필요 N건" 카운트가 실제 검증 결과와 일치하는지 검증

- **Task 018: 교정 페이지 데이터 연동 및 저장 API 구현 (F005)**
  - 곡 트리 조회 API (`GET /api/songs/[songId]`) — 원본 이미지 서명 URL 포함
  - 교정 저장 API (`PATCH /api/songs/[songId]/correction`) — 섹션/줄/코드 일괄 upsert + 삭제 반영, 트랜잭션 처리
  - 저장 시 서버 측 Zod 재검증 및 코드 문법 재검증(클라이언트 검증 신뢰 금지)
  - **임시 저장(자동 저장) 구현** — 저장 없이 이탈해도 다음 접속 시 이어서 교정 가능(PRD 명시 요구)
  - 저장 완료 시 `songs.status → corrected`, 키/템포/박자 확정값 반영
  - 코드 드래그 이동 시 `char_offset`과 `beat_offset` 동기 재계산 로직 구현
  - 동시 편집 충돌 방지를 위한 버전/updated_at 기반 낙관적 잠금
  - 완료 기준: 교정 페이지에서 실제 API로 조회·저장이 동작하고, 임시 저장 후 재진입 시 이어서 교정 가능함

  #### 테스트 체크리스트
  - [ ] 코드 칩을 드래그로 이동하면 `char_offset`과 `beat_offset`이 함께 갱신되는가
  - [ ] 섹션 라벨 변경·병합·분할 후 저장·재조회 시 구조가 보존되는가
  - [ ] 저장하지 않고 이탈 후 재진입 시 임시 저장 내용이 복원되는가
  - [ ] 서버가 잘못된 beat 값·음수 오프셋을 거부하는가
  - [ ] 저장 중 네트워크 실패 시 사용자에게 명확한 에러가 표시되고 데이터가 유실되지 않는가
  - [ ] Playwright MCP: 교정 페이지 진입 → 코드 이동 → 가사 수정 → 섹션 재라벨 → 키/템포 확정 → 저장 → 편곡 설정 페이지 이동 E2E 검증
  - [ ] Playwright MCP: 교정 중 새로고침 후 임시 저장 복원 검증

- **Task 019: Tonal.js 기반 다악기 편곡 엔진 구현 (F006)**
  - 편곡 엔진 코어 작성 (`src/lib/arrangement/generate.ts` — 제품의 핵심 IP)
  - Tonal.js `Voicing` / `Voice-Leading`으로 코드 기호 → 음정 해석 및 코드 간 성부 연결
  - 장르 프리셋 4종 패턴 라이브러리 구축: `praise_upbeat`, `ccm_ballad`, `hymn_traditional`, `acoustic_intimate`
  - 악기별 패턴 생성기: 피아노 컴핑, 기타 스트로크/핑거피킹, 베이스 루트-5도, 드럼 그루브
  - **섹션별 다이나믹스/악기 구성 맵**(예: 1절은 피아노만, 후렴은 풀밴드)으로 체감 품질 향상
  - 출력은 beat 단위 노트 이벤트 JSON — smplr Sequencer에 직접 투입 가능한 형식으로 `instrument_tracks.notes`에 저장
  - Inngest 잡 `generate-arrangement`로 비동기 실행 및 진행 상태 노출
  - 편곡 생성 API (`POST /api/songs/[songId]/arrangements`) 및 프리셋 재생성 지원
  - 완료 기준: 4개 장르 프리셋 중 하나를 선택해 생성 요청 시 4악기 노트 이벤트가 beat 정렬된 상태로 저장되고 미리듣기로 확인 가능함

  #### 테스트 체크리스트
  - [ ] 생성된 노트 이벤트의 beat 위치가 `ChordEvent.beat_offset`과 정확히 정렬되는가
  - [ ] 코드 전환 지점에서 이전 코드 노트가 겹쳐 울리지 않는가
  - [ ] 4개 프리셋이 서로 구분 가능한 패턴을 생성하는가
  - [ ] 파싱 불가 코드가 포함된 곡에서 엔진이 크래시 없이 스킵·대체 처리하는가
  - [ ] 섹션별 악기 구성 맵이 실제 트랙에 반영되는가
  - [ ] Playwright MCP: 편곡 설정 페이지에서 프리셋 선택 → 생성 요청 → 완료 후 4개 악기 트랙 표시 E2E 검증

- **Task 020: 곡·세트리스트 CRUD API 및 홈 연동 (F011, F012)**
  - 곡 목록 API (`GET /api/songs`) — 상태·최신순 정렬, 페이지네이션
  - 세트리스트 CRUD API (`GET/POST/PATCH/DELETE /api/setlists`, `/api/setlists/[id]`)
    - `ListSetlistsResponse`에 세트리스트별 곡 수(예: `itemCount`) 필드 추가 — Task 007 홈 화면이
      목 구현체에서 `getById`로 N+1 재조회하던 임시 방편을 제거하고 목록 응답만으로 곡 수를 표시하게 함
  - 세트리스트 항목 API — 곡 추가/제거, `order_index` 재정렬, 항목별 `arrangement_id` 지정
  - 홈·세트리스트 구성 페이지의 목 리포지토리를 실제 API로 교체
  - 곡 삭제 시 연관 데이터(섹션/줄/코드/편곡/트랙/세트리스트 항목) 정리 정책 적용
  - 낙관적 UI 업데이트 및 실패 시 롤백
  - 완료 기준: 홈·세트리스트 구성 페이지가 목 데이터 없이 실제 API만으로 곡/세트리스트 생성·조회·정렬·삭제를 완결함

  #### 테스트 체크리스트
  - [ ] 드래그 정렬 후 `order_index`가 서버에 반영되고 재접속 시 유지되는가
  - [ ] 편곡이 없는 곡을 세트리스트에 추가할 때 적절히 차단·안내되는가
  - [ ] 세트리스트에 포함된 곡을 삭제할 때 항목이 정합성 있게 처리되는가
  - [ ] 타 사용자의 세트리스트 ID로 API 호출 시 403이 반환되는가
  - [ ] Playwright MCP: 홈 → 새 세트리스트 생성 → 곡 3개 추가 → 순서 변경 → 저장 → 새로고침 후 순서 유지 E2E 검증

- **Task 021: smplr 재생 엔진 어댑터 및 기본 재생 제어 구현 (F007)**
  - **`smplr` v1.0.0 API를 감싸는 얇은 자체 어댑터 레이어 작성** (`src/lib/playback/engine.ts`)
    - `docs/PLAN.md`의 의존성 리스크(단일 메인테이너 소규모 프로젝트) 완화 — 향후 교체 가능성 확보
  - `AudioContext` 사용자 제스처 활성화 및 악기 사운드폰트 로딩(피아노/기타/베이스/드럼)
  - `InstrumentTrack.notes` → smplr Sequencer 패턴 변환기
  - tick(beat) 기반 transport 구성, `tickToAudioTime()` 기반 beat↔초 환산 유틸
  - 재생 / 일시정지 / 구간 탐색(seek) 및 현재 재생 위치 구독 API
  - 악기별 볼륨 믹싱 및 편곡 설정 페이지 미리듣기 연결
  - 스케줄러 경계 타이밍 유닛 테스트 (이벤트 누락·중복 검증)
  - 완료 기준: 실제 편곡 트랙이 smplr 어댑터를 통해 재생/일시정지/탐색되며 스케줄러 유닛 테스트에서 이벤트 누락·중복이 0건임

  #### 테스트 체크리스트
  - [ ] 재생 시작 후 코드 변화가 악보 타이밍과 일치해 들리는가
  - [ ] 일시정지 후 재개 시 재생 위치가 정확히 이어지는가
  - [ ] 템포 변경 시 모든 악기 트랙이 동기 유지되는가
  - [ ] 사용자 제스처 없이 재생 시도 시 활성화 안내가 표시되는가
  - [ ] 스케줄러 유닛 테스트에서 이벤트 누락/중복이 0건인가
  - [ ] Playwright MCP: 편곡 설정 페이지 미리듣기 재생/일시정지 및 오디오 컨텍스트 상태 검증

- **Task 022: 마디 경계 정렬 실시간 구간 전환 엔진 구현 (F008)** *(MVP 최대 기술 난이도)*
  - **`smplr`의 `position` setter(seek)는 즉시형이므로, "현재 마디가 끝나는 시점까지 대기 후 이동"하는 지연 점프 래퍼를 직접 구현** (`src/lib/playback/section-jump.ts`)
    - 다음 마디 경계 tick 계산 → 해당 시점까지 점프 예약 → 경계 도달 시 seek 실행
  - **예약된 노트 잔향 처리**: lookahead 창(약 100~150ms) 내 이미 예약된 이전 구간 노트 대응 (마디 경계 정렬로 대부분 자연스럽게 흡수됨을 검증)
  - 섹션 전환 상태 머신 구현: `현재 섹션 → { 다음 섹션 / 구간 반복 / 특정 섹션 점프 / 다음 곡 }`
  - 구간 반복(루프) 모드: 섹션 끝에서 자동으로 섹션 시작으로 복귀, 토글 해제 시 다음 섹션으로 자연 진행
  - Line 단위 점프 지원 ("마지막 소절 반복" — 섹션보다 세밀한 단위의 동일 메커니즘)
  - 전환 예약 상태를 UI에 노출(펜딩 인디케이터), 예약 취소 지원
  - 실시간 재생 페이지의 가사 탭(절/후렴 이동)과 섹션 배지의 반복 토글을 엔진에 배선(Task 011의
    가사 중심 내비게이션 — 전용 "다음 섹션/점프" 버튼이 아니라 가사 탭 자체가 이 상태 머신을 구동한다)
  - 경계 타이밍 자동화 테스트 (예약 시점 → 실제 실행 tick의 오차 측정)
  - 완료 기준: 재생 중 다음 섹션/구간 반복/특정 섹션 점프가 항상 다음 마디 경계에서만 실행되고 오디오 끊김·잔향 없이 전환됨

  #### 테스트 체크리스트
  - [ ] 마디 중간에 가사의 다음 섹션을 탭해도 다음 마디 경계에서만 전환되는가
  - [ ] 전환 시점에 오디오 끊김·클릭 노이즈가 발생하지 않는가
  - [ ] 섹션 배지의 반복 토글 on/off가 즉시 반영되고 반복 시 이음매가 매끄러운가
  - [ ] 가사에서 임의 섹션(다른 곡 포함)을 연속 3회 빠르게 탭해도 상태 머신이 깨지지 않는가
  - [ ] 점프 예약 후 취소 시 원래 진행이 유지되는가
  - [ ] 전환 직전 예약된 이전 구간 노트가 잔향으로 남지 않는가
  - [ ] Playwright MCP: 재생 중 가사의 다음 섹션 탭 → 반복 토글 → 다른 곡 섹션 탭을 순차 실행하며 UI 표시와 엔진 상태 동기화 E2E 검증

- **Task 023: 무음 없는 다음 곡 전환 구현 (F009)**
  - 세트리스트 순서를 소비하는 재생 큐 구현 (`SetlistItem.order_index` + `arrangement_id`)
  - **곡 간 조·템포가 달라도 무음 간격 없이 이어지는 전환** — 다음 곡 transport를 미리 준비해 두고 경계에서 크로스 스위치
  - 곡 경계에서의 BPM 전환 처리 및 이전 곡 잔여 예약 노트 정리
  - 다음 곡의 악기 사운드폰트가 현재 곡과 다를 경우 사전 확보 보장(Task 024와 연동)
  - 마지막 곡 종료 시 예배 종료 처리 및 홈 복귀
  - 실시간 재생 페이지의 **가사 스크롤에서 다음 곡 섹션 탭** 및 세트리스트 위치 표시 배선
  - 완료 기준: 조·템포가 다른 두 곡 간 전환에서 측정 가능한 무음 구간 없이 다음 곡으로 이어지고, 마지막 곡 종료 시 홈으로 정상 복귀함

  #### 테스트 체크리스트
  - [ ] 곡 전환 시 오디오 무음 구간이 측정 가능한 수준으로 발생하지 않는가
  - [ ] 서로 다른 키·템포의 곡 간 전환에서 타이밍이 어긋나지 않는가
  - [ ] 곡 전환 직후 섹션 점프·반복이 정상 동작하는가
  - [ ] 세트리스트 마지막 곡에서 "다음 곡"이 비활성화되거나 종료 처리로 이어지는가
  - [ ] 전환 중 이전 곡의 노트가 새 곡 위에 겹쳐 울리지 않는가
  - [ ] Playwright MCP: 3곡 세트리스트로 곡1 → 곡2 → 곡3 순차 전환 및 곡 종료 후 홈 복귀 E2E 검증

- **Task 024: 반주 사전 로딩 구현 (F015)**
  - "예배 시작" 시 세트리스트 전 곡의 편곡 트랙 데이터 일괄 페치
  - 사용되는 전체 악기 사운드폰트 사전 다운로드 및 디코딩
  - 항목별 로딩 진행률 집계와 사전 로딩 UI 연결, **완료 시 재생 페이지로 자동 진행**
  - 로딩 실패 항목 재시도 및 부분 실패 시 진행 여부 선택 안내
  - 메모리 사용량 관리(대용량 세트리스트 대비) 및 로딩 결과 세션 캐시
  - 완료 기준: "예배 시작" 클릭 후 전 곡의 트랙·사운드폰트 로딩이 완료된 시점에만 재생 페이지로 자동 진입하며, 진입 직후 첫 재생에 지연이 없음

  #### 테스트 체크리스트
  - [ ] 로딩 완료 전에는 재생 화면으로 진입되지 않는가
  - [ ] 로딩 진행률이 실제 자산 로딩 상태를 반영하는가
  - [ ] 네트워크 지연·중단 시 재시도로 복구되는가
  - [ ] 로딩 완료 후 첫 재생 버튼 입력에 즉시 소리가 나는가(지연 없음)
  - [ ] Playwright MCP: 세트리스트 "예배 시작" → 사전 로딩 화면 → 자동 재생 페이지 진입 E2E 검증

- **Task 025: 재생 안정성 확보 구현 (F014)**
  - **Wake Lock API로 화면 꺼짐 방지**, 미지원 브라우저 폴백 및 활성 상태 UI 표시
  - `AudioContext` suspend 감지 후 **자동 resume** (iOS Safari 화면 잠금·백그라운드 전환 대응)
  - 재생 시작 전 사용자 제스처 기반 오디오 활성화 동작 명시적 배치
  - 페이지 가시성 변화(`visibilitychange`)·오디오 디바이스 변경 대응
  - 예기치 못한 오류 시 재생 상태 복구 및 사용자 알림
  - 데스크톱 Chrome / 모바일 Safari 실기기 장시간 재생 검증
  - 완료 기준: 데스크톱 Chrome·모바일 Safari 실기기에서 30분 연속 재생 및 화면 잠금·백그라운드 전환 후에도 오디오 끊김 없이 자동 재개됨

  #### 테스트 체크리스트
  - [ ] Wake Lock 획득/해제가 정상 동작하고 미지원 시 안내가 표시되는가
  - [ ] 백그라운드 전환 후 복귀 시 오디오가 자동 재개되는가
  - [ ] 화면 잠금 후 해제 시 재생 위치가 유지되는가
  - [ ] 30분 연속 재생 중 끊김이 발생하지 않는가
  - [ ] 이어폰 연결/해제 시 재생이 중단되지 않는가
  - [ ] Playwright MCP: 탭 비활성화 → 재활성화 시나리오에서 오디오 컨텍스트 상태와 재생 위치 복원 검증

- **Task 026: 핵심 기능 통합 테스트**
  - Playwright MCP를 사용한 **PRD 사용자 여정 10단계 전체 플로우 E2E 테스트**
    - 로그인 → 홈 → 업로드 → 추출 → 교정 → 편곡 → 세트리스트 → 사전 로딩 → 실시간 재생 → 홈 복귀
  - 분기 플로우 테스트: "저장만 하고 나중에" 경로, 교정 임시 저장 후 재진입 경로
  - 데이터 정합성 통합 검증: 추출 → 교정 → 편곡 → 재생까지 beat 값이 손실 없이 전파되는지
  - 에러 핸들링 시나리오: 추출 실패, 업로드 실패, API 5xx, 네트워크 단절, 인증 만료
  - 엣지 케이스: 섹션 1개짜리 곡, 코드 없는 줄, 매우 긴 곡, 세트리스트 1곡, `needs_review` 다수 발생 곡
  - 회귀 테스트 스위트 정리 및 실행 절차 문서화
  - 완료 기준: PRD 사용자 여정 10단계 및 분기 플로우·에러 시나리오·엣지 케이스가 실제 백엔드 기준으로 모두 통과함

  #### 테스트 체크리스트
  - [ ] 전체 사용자 여정 10단계가 실제 백엔드로 처음부터 끝까지 완주되는가
  - [ ] 각 단계 실패 시 사용자가 복구 가능한 경로가 제공되는가
  - [ ] 엣지 케이스 곡에서 크래시나 무한 로딩이 발생하지 않는가
  - [ ] 인증 만료 상태에서 진행 중이던 작업이 안전하게 처리되는가
  - [ ] 브라우저 새로고침 후 각 페이지가 올바른 상태로 복원되는가

---

### Phase 4: 고급 기능 및 최적화

- **Task 027: 추출 정확도 및 비용 최적화**
  - 실제 한국 교회 리드시트 10장 이상으로 추출 정확도 실측 (텍스트 추출 vs 마디/박자 구조 추출 신뢰도 분리 측정)
  - **모델 등급 비교 테스트(Sonnet 5 vs Haiku 4.5)로 비용/정확도 균형점 확정**
  - 프롬프트 캐싱 적용 및 이미지 해상도 최적화로 곡당 토큰 비용 절감
  - 프롬프트 반복 개선 및 실패 사례 회귀 세트 구축
  - 곡당 추출 비용·소요 시간 계측 대시보드(내부용)
  - 완료 기준: 곡당 추출 비용 목표치 달성 및 교정 소요 시간 5분 이내

- **Task 028: 성능 최적화 및 자산 캐싱 전략**
  - 사운드폰트·편곡 트랙 데이터의 브라우저 캐싱 및 R2 캐시 헤더 최적화
  - 초기 번들 크기 축소(오디오 엔진 코드 스플리팅, 교정 에디터 지연 로딩)
  - 곡 트리 조회 쿼리 최적화 및 응답 캐싱, 홈 목록 페이지네이션
  - 이미지 최적화(`next/image`) 및 교정 페이지 이미지 뷰어 렌더링 성능 개선
  - Core Web Vitals 및 재생 페이지 입력 반응 지연 측정·개선
  - 완료 기준: 모바일 환경에서 재생 페이지 버튼 입력 반응 지연이 체감되지 않음

- **Task 029: 모니터링·로깅 및 CI/CD 파이프라인 구축**
  - Sentry 연동(프론트엔드 + Route Handler + Inngest 잡) 및 오디오 엔진 전용 에러 태깅
  - 구조화 로깅 및 추출/편곡 잡 성공률·소요 시간 메트릭 수집
  - GitHub Actions CI: 타입체크, 린트, 유닛 테스트, Playwright E2E 실행
  - Vercel 프리뷰/프로덕션 배포 파이프라인 및 Supabase 마이그레이션 자동 적용
  - **Inngest 무료 티어 동시 실행 5스텝 제한 모니터링 및 유료 전환 임계치 알림**
  - 완료 기준: PR마다 CI가 통과해야 병합 가능하며 프로덕션 에러가 실시간 추적됨

- **Task 030: 프로덕션 배포 및 실사용 리허설 검증**
  - 프로덕션 환경 변수·시크릿 구성, R2 CORS·버킷 정책, Supabase 프로덕션 마이그레이션
  - **브라우저 지원 범위 확정 및 공지** (TailwindCSS v4 요구사항 — Safari 16.4+ / Chrome 111+ / Firefox 128+, 교회 노후 기기 대응 여부 확인)
  - 데스크톱 Chrome + 모바일 Safari 실기기에서 **실제 예배 시뮬레이션 리허설**(3곡 이상 세트리스트, 즉흥 구간 전환 포함)
  - 실제 찬양 인도자 대상 사용성 테스트 및 피드백 반영
  - 운영 문서 정리: 장애 대응 절차, 백업 정책, 사용자 온보딩 가이드
  - 완료 기준: 실제 인도자가 한 번의 예배 전 과정을 앱만으로 진행하고 오디오 끊김 0건

---

## 백로그 (우선순위 미정)

Task 번호를 부여하지 않은, 확정되지 않은 향후 아이디어 메모.

- **세트리스트 곡 간 조 전환 간주 제안 (선택 기능)**: 세트리스트에서 인접한 두 곡의 조(key)가 다르면,
  다음 곡 조의 딸림화음(V)을 전환 간주 코드로 기본 제안하고(예: G→D 전환 시 A) 사용자가 켜고 끄거나
  직접 다른 코드로 바꿀 수 있게 하는 기능. 필수 아님.
  - 화면: Task 010 세트리스트 구성 화면에 곡 사이 UI로 추가 (기존 구조 변경 없이 삽입 가능)
  - 데이터: `setlist_items`에 nullable 필드 추가로 충분 (Task 013 스키마 설계 시 지금 미리 반영할 필요 없음)
  - 재생: 실제로 소리가 나가려면 Task 019(편곡 생성)에서 해당 구간 악기 트랙 생성이 필요

---

## 진행 현황 요약

| Phase | Task 수 | 상태 |
|---|---|---|
| Phase 1: 애플리케이션 골격 구축 | 4 (Task 001~004) | ✅ 완료 |
| Phase 2: UI/UX 완성 (더미 데이터) | 8 (Task 005~012) | ✅ 완료 |
| Phase 3: 핵심 기능 구현 | 14 (Task 013~026) | 진행 중 (3/14) |
| Phase 4: 고급 기능 및 최적화 | 4 (Task 027~030) | 대기 |
| **합계** | **30** | **15/30 완료** |

## 기능 커버리지 매핑

| 기능 ID | 기능명 | 담당 Task |
|---|---|---|
| F001 | 악보 이미지 업로드 | Task 008, 015 |
| F002 | 가사/코드/조표 추출 | Task 016 |
| F013 | 마디/박자 구조 추출 | Task 016 |
| F003 | 코드 문법 자동 검증 | Task 017 |
| F004 | 섹션 자동 추론 | Task 017 |
| F005 | 수동 교정 | Task 009, 018 |
| F006 | 다악기 편곡 자동 생성 | Task 010, 019 |
| F007 | 곡 재생 제어 | Task 011, 021 |
| F008 | 실시간 구간 전환 | Task 022 |
| F009 | 다음 곡 전환 | Task 023 |
| F010 | 기본 인증 | Task 007, 014 |
| F011 | 세트리스트 구성 | Task 010, 020 |
| F012 | 곡/세트리스트 목록 조회 | Task 007, 020 |
| F014 | 재생 안정성 확보 | Task 025 |
| F015 | 반주 사전 로딩 | Task 024 |

## 핵심 리스크 및 완화

| 리스크 | 영향 | 완화 방안 | 관련 Task |
|---|---|---|---|
| 비전 LLM의 마디/박자 구조 추출 정확도 (공간 추론·카운팅은 공식 문서상 명시된 한계) | 반주 타이밍 전체가 어긋남 | 텍스트 추출과 구조 추출 호출 분리, Structured Outputs 강제, self-consistency 체크, 교정 UI를 최종 방어선으로 설계, Phase 4에서 실측 | 016, 017, 018, 027 |
| `smplr` seek의 즉시형 동작 (마디 경계 정렬 미지원) | 구간 전환 시 박자 어긋남 | 다음 마디 경계 tick을 계산해 점프를 지연시키는 자체 래퍼 구현 | 022 |
| lookahead 창 내 예약 노트 잔향 | 전환 직후 이전 구간 소리 잔존 | 마디 경계 정렬로 흡수 + 잔여 예약 정리 로직 | 022, 023 |
| `smplr` 단일 메인테이너 의존성 | 라이브러리 중단 시 재생 엔진 전면 재작성 | v1.0.0 고정 + 얇은 자체 어댑터 레이어로 교체 가능성 확보 | 021 |
| iOS Safari의 AudioContext suspend | 예배 중 반주 중단 | Wake Lock + suspend 감지 자동 resume + 실기기 장시간 검증 | 025 |
| R2가 브라우저 form POST 업로드 미지원 | 업로드 구현 실패 | presigned PUT URL + CORS 설정 방식 채택 | 015 |
| 비전 LLM 호출 비용의 선형 증가 | 스케일링 시 최대 변동비 | 프롬프트 캐싱, 이미지 해상도 최적화, 모델 등급 재검토 | 027 |
| Next.js 16 미들웨어 파일명 변경(`proxy.ts`) | Clerk 보호 라우트 미동작 | Phase 1에서 파일명 규약 확인 후 배선 | 004, 014 |
| TailwindCSS v4 브라우저 요구사항 | 교회 노후 기기에서 화면 깨짐 | 지원 범위 확정 및 배포 전 실기기 확인 | 030 |
