// 로그인/회원가입 폼이 공유하는 필드 단위 Zod 조각.
// 두 폼은 필드 구성 자체가 다르므로(이름 필드 유무) 스키마 전체를 공유하진 않지만,
// 이메일/비밀번호 검증 규칙과 에러 문구가 양쪽에 글자 그대로 중복되는 걸 막기 위해
// 필드 단위로만 뽑아둔다. 실제 인증 연동(Task 014)에서 비밀번호 정책이 바뀌면 여기만 고치면 된다.

import { z } from "zod";

export const emailField = z.email("올바른 이메일 형식이 아닙니다.").min(1, "이메일을 입력하세요.");

export const passwordField = z.string().min(8, "비밀번호는 8자 이상이어야 합니다.");
